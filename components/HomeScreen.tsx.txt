import React from 'react';

interface HomeScreenProps {
  onStart: () => void;
}

const HomeScreen: React.FC<HomeScreenProps> = ({ onStart }) => {
  return (
    <div className="bg-white rounded-xl shadow-lg p-8 text-center">
      <h1 className="text-4xl font-bold text-slate-700 mb-2">
        Classroom Sampler
      </h1>
      <p className="text-slate-500 mb-8">
        Designed for Grades 6–8
      </p>
      <button
        onClick={onStart}
        className="w-full bg-blue-600 text-white font-bold py-3 px-4 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 transition-transform transform hover:scale-105"
      >
        Start Sampling
      </button>
    </div>
  );
};

export default HomeScreen;